package com.example.mapping.service;

import com.example.mapping.dto.CourseDto;
import com.example.mapping.dto.StudentDto;

public interface CourseService {
    CourseDto addCourse(CourseDto requestDto);
}
